import { Lato, Krona_One } from "next/font/google";

export const lato = Lato({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});

export const krona_one = Krona_One({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});
